"use strict";

// This is the JavaScript file we edit
var printOut = function printOut() {
  console.log(document.getElementById('/'));
}; //if (onclick = number)


var getInput = function getInput() {
  var zero = document.getElementById('0');
  console.log(zero); //store that number in a string

  var numberString = document.getElementById('numberfromhtml');
  ; //if next click is number... keep in this loop, add it to the string
  //make the string a number
  //else
  //GetOperator = () => {
  //read in the GetOperator
  //loop back to the top to get more numbers
  //}
};

calculate = function calculate() {};

clear = function clear() {};